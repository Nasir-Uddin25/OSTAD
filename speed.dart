import 'assignmet_1.dart';
// use encapsulation to prevent direct access to _speed
void main()
{
  //In the main(), create an object Car, set the speed and call the move() method
  Car obj = Car();
  obj.move();
  obj.setSpeed = 50.3;
}
